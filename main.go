package main

func main() {
	// server, err := wire.InitRBACServer(".", "app", "env", "rbac_with_tenant_model.conf")
	// if err != nil {
	// 	log.Fatal("init RBAC server failed err is ", err)
	// }

	// fmt.Println("server", server)
}
